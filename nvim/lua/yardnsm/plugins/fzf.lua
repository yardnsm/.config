return {
  "ibhagwan/fzf-lua",

  dependencies = { "nvim-tree/nvim-web-devicons" },

  cmd = "FzfLua",

  opts = {
    "telescope",
  },

  keys = {
    -- TODO file, live greap, git status, C-p, grep string
  }
}
